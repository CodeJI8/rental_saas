const prisma = require("../config/prisma");


const properties= (req, res) => {

    try {
         const {name ,   address,  total_units} = req.body
        
    } catch (error) {
        
    }







}